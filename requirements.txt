# Pinned to the audited versions for reproducible, supply-chain-safe image builds.
# (python-multipart 0.0.30 is well above the CVE-2024-53981 fix line of 0.0.18.)
google-adk==2.1.0
mcp==1.27.2
python-dotenv==1.2.2
fastapi==0.136.3
uvicorn==0.48.0
litellm==1.86.2          # BYOK: drive Gemini / any OpenAI-compatible endpoint
openpyxl==3.1.5          # read uploaded ADS/MDI batch templates
python-multipart==0.0.30 # FastAPI file uploads (/api/uploads)
argon2-cffi==25.1.0      # platform-password hashing for the login gate
cryptography==48.0.0     # Fernet encryption-at-rest for the persisted WF token bundle
httpx==0.28.1            # used by server.py + wf_auth.py
psycopg[binary]==3.3.4   # Postgres backend (production); SQLite stays the dev/test default
